# JavaScript Standard

- Use explicit, readable code with strong typing where supported.
- Validate inputs at boundaries and handle failures explicitly.
- Keep modules small and focused with clear interfaces.
- Add tests for all business-critical logic changes.

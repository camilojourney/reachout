# Node.js API + Static Frontend Standard

- Keep architecture boundaries clear (UI, API, data, automation).
- Prefer composable modules over tightly coupled implementations.
- Preserve backward compatibility for existing callers when possible.
- Document behavior changes alongside implementation updates.

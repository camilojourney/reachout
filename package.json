{
  "name": "reachout",
  "version": "1.0.0",
  "description": "Networking and informational interview tracker for job seekers",
  "main": "api-server.js",
  "scripts": {
    "start": "node api-server.js",
    "dev": "node api-server.js",
    "test": "node --test --test-timeout=30000 tests/*.test.js",
    "test:coverage": "node --test --test-timeout=30000 --experimental-test-coverage tests/*.test.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "db:init": "node scripts/init-db.js"
  },
  "keywords": [
    "networking",
    "informational-interview",
    "job-search",
    "contacts",
    "outreach"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "express": "^4.21.0",
    "openai": "^6.21.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
